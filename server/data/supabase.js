/**
 * supabase.js — Stub module (Supabase removed, using Firebase Firestore).
 * Exports no-op helpers for backward compatibility.
 */

export function fromDbRecord(record) {
  return record;
}

export function toDbRecord(record) {
  return record;
}
