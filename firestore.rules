rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads/writes for demo (restrict in production)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
